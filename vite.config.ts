import { defineConfig } from 'vite';

// base = ім'я репозиторію на GitHub — потрібно для GitHub Pages
// (https://<user>.github.io/oborona-sichi/)
export default defineConfig({
  base: '/oborona-sichi/',
});
